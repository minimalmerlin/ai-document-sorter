# AI Document Sorter - Environment Configuration Template
# Copy this file to .env and customize the values for your setup

# ==================================================
# PATH CONFIGURATION
# ==================================================

# Directory where scanned documents will be placed for processing
# The application monitors this folder for new files
INBOX_PATH=/Users/yourusername/Documents/Inbox_Scan

# Root directory where sorted documents will be organized
# Subdirectories will be created automatically based on categories
TARGET_ROOT=/Users/yourusername/Documents/Sorted_Documents

# ==================================================
# OLLAMA / LLM CONFIGURATION
# ==================================================

# Ollama API endpoint URL
# Default: http://localhost:11434/api/generate
OLLAMA_URL=http://localhost:11434/api/generate

# Ollama model name to use for document analysis
# Examples: llama3.2, llama2, mistral, phi
# Make sure to pull the model first: ollama pull llama3.2
MODEL_NAME=llama3.2

# ==================================================
# PROCESSING CONFIGURATION
# ==================================================

# Minimum content length (in characters) before triggering OCR fallback
# If PDF text extraction yields fewer characters, OCR will be attempted
MIN_CONTENT_LENGTH=50

# Tesseract OCR language codes (plus-separated)
# Examples: eng, deu, fra, deu+eng (German and English)
# Available languages depend on your Tesseract installation
OCR_LANGUAGES=deu+eng

# Maximum content length sent to LLM for analysis (characters)
# Longer content will be truncated to avoid API limits and reduce processing time
CONTENT_PREVIEW_LENGTH=2000

# Delay in seconds after file creation before processing
# Helps ensure files are fully written and not locked
FILE_STABILIZATION_DELAY=2.0

# ==================================================
# LOGGING CONFIGURATION
# ==================================================

# Logging level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Optional: Path to log file (leave empty for console-only logging)
# Example: /Users/yourusername/Documents/document-sorter.log
LOG_FILE=
